import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:iomedicamentos/app/modules/login/splash/splash_controller.dart';
import 'package:iomedicamentos/app/modules/login/login_module.dart';

void main() {
  initModule(LoginModule());
  // SplashController splash;
  //
  setUp(() {
    //     splash = LoginModule.to.get<SplashController>();
  });

  group('SplashController Test', () {
    //   test("First Test", () {
    //     expect(splash, isInstanceOf<SplashController>());
    //   });

    //   test("Set Value", () {
    //     expect(splash.value, equals(0));
    //     splash.increment();
    //     expect(splash.value, equals(1));
    //   });
  });
}

import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:iomedicamentos/app/modules/sobre/sobre_page.dart';

void main() {
  testWidgets('SobrePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SobrePage(title: 'Sobre')));
    //  final titleFinder = find.text('Sobre');
    //  expect(titleFinder, findsOneWidget);
  });
}
